{
  "name": "muxi_blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "sass --watch --no-cache scss/pages/:../static/css/",
    "watch:css":"watch 'npm run build:css' scss/*",
    "build":"npm run build:css",
    "watch":"npm run watch:css && npm run livereload",
    "livereload":"live-reload --port 5050 ./"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "live-reload": "^1.1.0",
    "mockjs": "^0.1.10",
    "sass": "^0.5.0",
    "watch": "^0.16.0"
  }
}
